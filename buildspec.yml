version: 0.2
 
phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      pip install aws-sam-cli
 
  build:
    commands:
      sam build
 
artifacts:
  base-directory: .aws-sam/build
  files:
    - template.yaml